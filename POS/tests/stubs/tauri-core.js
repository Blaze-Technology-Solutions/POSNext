// Test stub for @tauri-apps/api/core. Real implementation lives in the Tauri
// shell at runtime; vitest aliases this file so vite can resolve the import.
export const invoke = async () => {
	throw new Error("invoke() is not available outside Tauri runtime")
}
